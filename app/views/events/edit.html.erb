<br>
<p><%= link_to "日程一覧に戻る", days_path %></p>
<h3>
  <%= @day.start.month %>月<%= @day.start.day %>日～
  <%= @day.finish.month %>月<%= @day.finish.day %>日
</h3>
<p>ログイン中のユーザー：<%= current_user.name %></p>
<hr>
<h3>催事等編集フォーム</h3>
<%= form_with model:[@day, @event], url: day_events_path(@day.id), method: :patch do |f| %>
  <table class = "table table-sm table-bordered table-hover" >
    <tr class="table-secondary">
      <%= render "jobs/day", day:@day %>
      <th style="width: 25% "></th>
    </tr>
    <tr class="table-secondary">
      <%= render "jobs/week", day:@day %>
    </tr>
    <tbody>
      <tr>
        <td></td>
        <td>催事等</td>
        <td><samll><%= f.text_area :time1 ,cols: 10 %></samll></td>
        <td><%= f.text_area :time2 ,class: 'form-control' %></td>
        <td><%= f.text_area :time3 ,cols: 10 %></td>
        <td><%= f.text_area :time4 ,cols: 10 %></td>
        <td><%= f.text_area :time5 ,cols: 10 %></td>
        <td><%= f.text_area :time6 ,cols: 10 %></td>
        <td><%= f.text_area :time7 ,cols: 10 %></td>
        <td class="text-left">
          <%= link_to new_day_job_path(@day.id) do %><button class="btn btn-link btn-sm">キャンセル</button><% end %>
          ｜
          <button type="submit" class="btn btn-link btn-sm ">保存</button>
        </td>
      </tr>
    </tbody>
  </table>
<% end %>
<hr>
<h3>シフト希望一覧</h3>
<%= render "jobs/shift_index", day:@day, user:@user, job_comment:@job_comment %>