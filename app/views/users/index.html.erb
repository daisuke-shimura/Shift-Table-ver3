<h2>ユーザー一覧</h2>
<% @user.each do |user| %>
    <li>
      <%= user.name %>
      <% if current_user.id == 1 || user.id == current_user.id %>
      　<%= link_to "退職", user_path(user.id), method: :delete, "data-confirm" => "本当に削除しますか？" %>
      <% end %>
    </li>
<% end %>