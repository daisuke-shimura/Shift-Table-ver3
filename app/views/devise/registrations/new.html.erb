<p>
  <%= link_to root_path do %>
    <i class="fa-solid fa-arrow-left-long"> 戻る</i>
  <% end %>
</p>
<h2>新規登録</h2>

<%= form_with model: @user,url: user_registration_path do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

    <!--%= f.label :email %-->
    <!--%= f.email_field :email, autofocus: true, autocomplete: "email" %-->


  <div class="field">
    <!--%= f.label :password %-->
    <!--% if @minimum_password_length %-->
    <!--%= @minimum_password_length %> characters minimum -->
    <!--% end %-->
    <%= f.hidden_field :password, autocomplete: "new-password" , value: "aiueoa" %>
  </div>

  <div class="field">
    <!--%= f.label :password_confirmation %--><br />
    <%= f.hidden_field :password_confirmation, autocomplete: "new-password" , value: "aiueoa" %>
  </div>

  <div class="actions">
    <button type="submit" class="btn btn-primary btn-sm">登録</button>
  </div>
<% end %>

<!--%= render "devise/shared/links" %-->
